# Process note

Same workflow as Part I. The one new failure: the first three regenerations returned `alpha` at max nodes (fail-hard), which gave estimates that differed from Part I by one on two boards. Writing "always return best, never alpha or beta" fixed it. Six regenerations total. Individual submission.
