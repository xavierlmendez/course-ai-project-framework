# Process note

Four regenerations. Three of them returned the swapped board — the program was right and the output was wrong, which the public suite catches immediately but which I would not have noticed by reading the code. Adding the sentence "the printed board and the output file use the ORIGINAL colours" as its own rule, rather than a clause inside the search paragraph, was what fixed it. Individual submission.
