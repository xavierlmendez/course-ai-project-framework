# Process note

Six regenerations. Two returned the swapped board, one hopped at `<= 3`, and one applied the hopping test to the *unswapped* piece count, so Black's own three-piece rule never fired. The last was the hardest to see: the estimates were plausible and only two of the public cases changed. Saying "everything inside the search runs on the swapped board, including the piece counts" fixed it. Individual submission.
