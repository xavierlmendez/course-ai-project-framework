Part IV (MiniMaxOpeningImproved, MiniMaxGameImproved): built the same way as part-I; not populated in this thin example. Because the improved estimation is student-defined, the equivalence policy is 'valid' (legal move, correct format) plus a 'differs from baseline' category, and the written component's twist-specificity question maps onto the original MyStaticEstimation.txt requirement.
