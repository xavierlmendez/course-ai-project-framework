# Process note

Four regenerations. The one that cost time added the mill and threat terms *before* the terminal tests, so a won position returned 10000 plus a mill bonus and the comparison against other won positions went the wrong way. Restating the order as a numbered rule fixed it. Individual submission.
