# Process note

Five regenerations. The first two produced a program that imported the baseline estimation and added the mill term only at the root, which changes nothing, because the root's value comes from the leaves. Saying "every call to the static estimation calls the improved function" fixed it. The third counted a line with two White and one Black as a threat. Individual submission.
