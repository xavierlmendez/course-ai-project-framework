[
 {
  "id": "J1-J1-4",
  "judge": "J1",
  "severity": "major",
  "file": "templates/ta-runbook.md:58",
  "claim": "The appeal command in the runbook is silently skipped by the runner, and even if run it would use the same seed as grading slot 2, which by the framework's own definition of reproducible yields the same result.",
  "evidence": "Reproduced: runs/GHI111222/k2.json {\"complete\": true} then `runner.py ... --run-tag appeal --slot 2 --dry-run` \u2192 \"GHI111222 k2: done, skipping\" (runner.py:247-250); framework.md:143 \"same weights, seed, temperature, and wrapper prompt produce the same result\"; framework.md:145 \"one additional regeneration at the middle temperature\"",
  "failure": "Granted appeal \u2192 runner writes nothing, grade.py unchanged; student promised an extra run (handout-type-b.md:110) gets none",
  "fix": "Write appeal records to appeal.json and define the appeal seed (new or fourth secret seed)",
  "reproduction": "Fixture in scratchpad (read-only w.r.t. the repo), using the repo's own example project:\n\n  mkdir -p $S/subs/GHI111222 $S/runs/GHI111222 $S/tests/hidden\n  cp examples/03-serialization-type-b-research/project.json $S/\n  cp examples/03-serialization-type-b-research/seeds.secret.json $S/   # {\"seeds\": [4127, 88213, 190531]}\n  echo x > $S/subs/GHI111222/SPEC.md\n  echo '{\"complete\": true, \"slot\": 2, \"s\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J1-J1-11",
  "judge": "J1",
  "severity": "minor",
  "file": "templates/ta-runbook.md:9",
  "claim": "The slot-model check greps a string that never appears in slot names.",
  "evidence": "ta-runbook.md:9 `ollama list | grep ref-slot`; runner.py:61 slot_prefix = \"ref-\" + name + \"-slot\" (e.g. ref-morris-p1-slot1)",
  "failure": "TA sees no output on Day 0 and concludes the slots are missing",
  "fix": "`ollama list | grep -- -slot`",
  "reproduction": "1. Read templates/ta-runbook.md:9 \u2192 \"Confirm the Ollama server is reachable and the slot models exist: `ollama list | grep ref-slot`.\"\n2. Read tools/runner.py:61 \u2192 `p.setdefault(\"slot_prefix\", \"ref-\" + p.get(\"name\", \"project\") + \"-slot\")`; line 105 \u2192 `name = f\"{p['slot_prefix']}{i}\"`.\n3. `python3 -c \"print('ref-' + 'project' + '-slot')\"` \u2192 `ref-project-slot` (default case still not `ref-slot`).\n4.\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J1-J1-12",
  "judge": "J1",
  "severity": "minor",
  "file": "templates/ta-runbook.md:11",
  "claim": "Pair submission identifiers use three different separators across deliverables.",
  "evidence": "ta-runbook.md:11 `submissions/<id1>-<id2>/`; examples/04 README.md:97 and variants.csv:4 `MNO777888+PQR999000`; ledger_server.py:101 joins with \"+\"",
  "failure": "grade.py keys rows by directory name, status.csv by whatever the TA typed; a pair keyed `ABC-DEF` in one file and `ABC+DEF` in another gets no total",
  "fix": "Fix one separator (`+`) everywhere",
  "reproduction": "Read-only fixture under the scratchpad, exercising the real tools/grade.py:\n\n  mkdir -p j112/runs/MNO777888-PQR999000   # directory named per ta-runbook.md:11 (hyphen)\n  project.json: {\"categories\": {\"base\": {\"weight\": 1}, \"twist\": {\"weight\": 1}}, \"hidden_points\": 70, \"milestone_points\": 10, \"written_points\": 20}\n  runs/MNO777888-PQR999000/a.json: {\"complete\": true, \"type\": \"A\", \"tests\": {\"solutio\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J2-J2-4",
  "judge": "J2",
  "severity": "major",
  "file": "tools/grade.py:44",
  "claim": "A category declared in project.json but with zero hidden tests still adds its weight to the denominator, penalising everyone.",
  "evidence": "Fixture with empty `ghost` category: perfect undergrad \u2192 `hidden_score 46.67`; perfect grad with empty grad_x too \u2192 `35.0`",
  "failure": "Professor typo or an empty/missing category dir (shipped part-III and part-IV have no tests/) \u2192 every student loses that weight fraction silently.",
  "fix": "Skip categories with total==0 from den, or exit with an error.",
  "reproduction": "Fixture written to the scratchpad (read-only w.r.t. the repo):\n\nproject.json \u2014 categories: basic{w1}, twist_x{w1,twist}, ghost{w1}, grad_g{w1,grad_only}, grad_x{w1,grad_only}; hidden_points 70.\nruns/AAA111111/a.json and runs/BBB222222/a.json \u2014 identical: {\"type\":\"A\",\"complete\":true,\"tests\":{\"solution_started\":true,\"categories\":{\"basic\":{\"pass\":5,\"total\":5},\"twist_x\":{\"pass\":4,\"total\":4},\"grad_g\":{\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J2-J2-8",
  "judge": "J2",
  "severity": "major",
  "file": "tools/sandbox/entrypoint.sh:9",
  "claim": "`chown -R runner:runner /work` on a bind mount changes host ownership to uid 1001 on Linux, so runner.py:207 `shutil.rmtree(workdir)` fails on resume.",
  "evidence": "entrypoint.sh:9 `chown -R runner:runner /work`; runner.py:206-207 rmtree before copytree; README:96 names Linux as a grading host",
  "failure": "Linux grading machine, rerun after a crash or appeal \u2192 PermissionError on runs/<id>/k1-work; batch aborts. Not reproduced here (macOS Docker Desktop masks uid mapping).",
  "fix": "Run the container with `--user 1001` and drop the chown.",
  "reproduction": "Read the sources first:\n\n- `tools/sandbox/entrypoint.sh:9` \u2192 `chown -R runner:runner /work 2>/dev/null || true`\n- `tools/sandbox/Dockerfile` \u2192 `useradd -m -u 1001 -s /bin/bash runner`; no `USER` line, `ENTRYPOINT [\"/usr/local/bin/entrypoint.sh\"]`\n- `tools/runner.py:122-137` `docker_base()` \u2192 `[\"docker\",\"run\",\"--rm\",\"-i\",\"-v\", f\"{os.path.abspath(workdir)}:/work\", ...]`, no `--user`\n- `tools/runner.\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J3-J3-5",
  "judge": "J3",
  "severity": "blocker",
  "file": "templates/ta-runbook.md:50",
  "claim": "The written and milestone components have no reproducible path: the runbook has TAs hand-enter dimension scores into gradebook.csv, never uses grade.py's --written/--milestone inputs, and no step verifies the student-submitted milestone record.",
  "evidence": "runbook:50 \"Enter the four (or three) dimension scores in gradebook.csv\"; grade.py:13-14 expects written.csv/milestone.csv; rubric.md:7 \"Runner JSON record submitted by the student: public suite pass == total \u2192 10\"; no milestone step in the runbook.",
  "failure": "Two TAs scale 9\u219220 and 12\u219220 by hand with different rounding; a student edits `pass` in their own runner JSON and collects the 10 points, nothing checks it.",
  "fix": "Runbook: fill written.csv/milestone.csv and rerun grade.py; TA re-runs the public suite for the milestone.",
  "reproduction": "Fixture (read-only against the repo) in /private/tmp/.../scratchpad/j3j3_5: project.json copied from examples/02-astar-type-a/, one run record runs/AAA000001/k1.json (complete, type A, categories basic 3/3, walls 2/3, unreachable 1/2, twist_long_run 1/2, twist_detour 0/2), status.csv `AAA000001,graded,0,`, plus written.csv `AAA000001,3,2,2` and milestone.csv `AAA000001,1`.\n\nRunbook command (ta-run\u2026",
  "reason": "blocker third refuter did not run"
 },
 {
  "id": "J6-J6-5",
  "judge": "J6",
  "severity": "major",
  "file": "templates/ta-runbook.md:9",
  "claim": "`ollama list | grep ref-slot` can never match the slot names the runner creates, and nobody is told to run --create-slots after seeds arrive.",
  "evidence": "runner.py:61 slot_prefix = `ref-<name>-slot` \u2192 `ref-morris-p1-slot1`; `grep ref-slot` exit=1.",
  "failure": "Day 0 check fails or is skipped; Day 1 evening every regeneration errors on an unknown model and scores zero.",
  "fix": "`grep ref-` and add step: `python3 tools/runner.py --project project.json --create-slots`.",
  "reproduction": "Read templates/ta-runbook.md:9 \u2192 `3. Confirm the Ollama server is reachable and the slot models exist: `ollama list | grep ref-slot`.`\n\nNaming, from tools/runner.py:\n  61: p.setdefault(\"slot_prefix\", \"ref-\" + p.get(\"name\", \"project\") + \"-slot\")\n  105: name = f\"{p['slot_prefix']}{i}\"\n  209: slot_model = f\"{p['slot_prefix']}{slot}\"\n\nChecked every project.json for an override (read-only, python3 json\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J7-J7-8",
  "judge": "J7",
  "severity": "minor",
  "file": "examples/03-serialization-type-b-research/resource/index.md:27",
  "claim": "The worked example prints a wrong 'Output' hex (trailing 0a) and retracts it two lines later.",
  "evidence": "line 27 \"{\"hex\": \"...6c313a617465650a\"}\"; line 29 \"(The trailing `0a` is not part of the output; the exact expected string is ...61746565`.)\"; reference solution prints the line-29 string",
  "failure": "A small model told to 'compare with the expected string printed there' can copy the first string and fail its self-check on the twist_length_prefix/key_order cases it would otherwise pass.",
  "fix": "Show only the correct hex on line 27.",
  "reproduction": "1. `sed -n '19,29p' examples/03-serialization-type-b-research/resource/index.md` \u2014 line 27 shows hex ending `...313a617465650a`; line 29 says the trailing `0a` is not part of the output and gives `...313a61746565`.\n2. `python3 -c \"print('d1:zi-3e3:cown4:eggsa:01234567894:spaml1:atee'.encode().hex())\"` \u2192 `64313a7a692d3365333a636f776e343a65676773613a30313233343536373839343a7370616d6c313a61746565` (n\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J8-J8-5",
  "judge": "J8",
  "severity": "minor",
  "file": "README.md:36",
  "claim": "CLAUDE.md, CONTRIBUTING.md, .claude/agents/, CI and pre-commit are absent, and the README has no test/lint command for inheritors; most of the standards' Python tooling (uv, ruff, pyproject, hypothesis, pytest, pre-commit) does not transfer to a stdlib-only tools directory promised as readable in one sitting (tools/README.md:3).",
  "evidence": "checks WARN rows for all five files; `for f in \u2026; do [ -e ]` \u2192 all absent; every tools/*.py exceeds line length 100 (max 110\u2013147)",
  "failure": "A TA or a future Claude Code session has no stated commands, no decision index, and no gate before editing grading code; adopting uv/ruff verbatim would add a toolchain TAs must install.",
  "fix": "Write a 30-line CLAUDE.md + CONTRIBUTING adapted to stdlib (`python3 -m unittest`, `python3 -m py_compile tools/*.py`) and a CI job running those plus `scripts/review_checks.py --no-network`.",
  "reproduction": "$ cd /Users/xaviermendez/develop/course-ai-project-framework\n$ for f in CLAUDE.md CONTRIBUTING.md .claude/agents .github/workflows .pre-commit-config.yaml pyproject.toml .github; do [ -e \"$f\" ] && echo \"PRESENT $f\" || echo \"ABSENT $f\"; done\nABSENT CLAUDE.md\nABSENT CONTRIBUTING.md\nABSENT .claude/agents\nABSENT .github/workflows\nABSENT .pre-commit-config.yaml\nABSENT pyproject.toml\nABSENT .github\n\n$ s\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J9-J9-6",
  "judge": "J9",
  "severity": "minor",
  "file": "xavis_projects/package.json:12",
  "claim": "The diff does two things: the course routes and an unrelated, half-staged Kafka removal.",
  "evidence": "`git status --porcelain`: `D  app/actions/kafka.js` (staged) while every other change is unstaged; package.json drops `@confluentinc/kafka-javascript`; package-lock.json -491 lines. `grep -rn actions/kafka app lib` -> no consumers; `ls app/api/kafka` -> absent at HEAD, so nothing new breaks (app/page.tsx:18-21 still links a Kafka viewer that fetches a nonexistent /api/kafka, pre-existing).",
  "failure": "`git commit` as-is commits only the Kafka deletion with none of the course work; the reviewer-agent 'one thing' check fails and the removal has no recorded reason.",
  "reproduction": "All commands run read-only from /Users/xaviermendez/develop/portfolioWebsite (the git root; the brief's path .../portfolioWebsite/xavis_projects is a subdirectory, so repo-relative paths carry the xavis_projects/ prefix).\n\n$ git status --porcelain\n M xavis_projects/README.md\nD  xavis_projects/app/actions/kafka.js\n M xavis_projects/app/projects/page.tsx\n M xavis_projects/package-lock.json\n M xavis_\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J9-J9-9",
  "judge": "J9",
  "severity": "minor",
  "file": "xavis_projects/app/lib/projectUtils/courseAiFramework/ledger.ts:15",
  "claim": "The default ledger path resolves inside the repo working tree and is not gitignored.",
  "evidence": "ledger.ts:15 `path.resolve(process.env.COURSE_LEDGER_PATH ?? './course-ledger.tsv')`; .gitignore has no `course-ledger*` rule; README.md:50 warns to keep it outside the repo.",
  "failure": "Running `next dev` without COURSE_LEDGER_PATH and then `git add .` commits a file of student IDs.",
  "fix": "Add `course-ledger*.tsv` to .gitignore or default to a path outside cwd.",
  "reproduction": "All commands run read-only except one transient empty-file creation that was removed (git status confirmed clean afterward).\n\n1) `sed -n '1,20p' /Users/xaviermendez/develop/portfolioWebsite/xavis_projects/app/lib/projectUtils/courseAiFramework/ledger.ts`\nLine 15: `  return path.resolve(process.env.COURSE_LEDGER_PATH ?? './course-ledger.tsv')`\n\n2) From the Next.js app root: `node -e \"const p=requir\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J10-J10-2",
  "judge": "J10",
  "severity": "major",
  "file": "framework.md:298",
  "claim": "Hybrid is offered as a professor option with no template, runner mode, or grading gate.",
  "evidence": "`grep -rni hybrid tools/ templates/` is empty; tools/runner.py:296 `choices=[\"A\", \"B\"]`.",
  "failure": "Professor chooses Hybrid from the options menu and has no handout, no runner invocation, and no way for grade.py to apply the public-suite reproducibility gate.",
  "reproduction": "Ran from /Users/xaviermendez/develop/course-ai-project-framework:\n\n1. `sed -n '298p' framework.md` \u2192 `| Project type | Type B | Type A; both in the same course; Hybrid (code graded, specification as a reproducibility gate) |` \u2014 line and claim confirmed.\n\n2. `grep -rni hybrid tools/ templates/` \u2192 no output, exit=1. Repo-wide `grep -rnil hybrid .` matches only: CONTEXT.md, framework.md, docs/grill/r\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J10-J10-4",
  "judge": "J10",
  "severity": "major",
  "file": "templates/calibration-checklist.md:12",
  "claim": "The Type A calibration path is undefined and contradicts framework.md.",
  "evidence": "framework.md:48 'Reference specification: Your own Type B specification' for every project; checklist:12 'or reference solution (Type A)'; example 02 has no reference/SPEC.md; boxes 21 (K ledger entries), 27 (canonical specification), 28 (empty specification) have no Type A meaning; `--submission reference/` with Type A copies reference/ whose root has no solve.py (runner.py:275).",
  "failure": "Type A professor runs the line-16 command, the correct reference scores 0 (entry absent at root), and three boxes cannot be ticked although line 3 says do not release until every box is ticked.",
  "reproduction": "All paths absolute under /Users/xaviermendez/develop/course-ai-project-framework.\n\n1) Quoted checklist (templates/calibration-checklist.md:12,16):\n   \"## Reference specification (Type B) or reference solution (Type A)\"\n   \"- [ ] `python3 tools/runner.py --project project.json --submission reference/ --run-tag calibration`\"\n\n2) Ran that command verbatim on the Type A example (examples/02-astar-type\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J10-J10-7",
  "judge": "J10",
  "severity": "major",
  "file": "templates/ta-runbook.md:24",
  "claim": "No document assigns who marks graduate students, and the runbook's status.csv format omits the column grade.py keys on.",
  "evidence": "ta-runbook.md:24 'status.csv (student_id,status,note)'; tools/grade.py:15 'status.csv: student_id,status,grad,note'; framework.md:263-271 workflow has no roster step.",
  "failure": "TA follows the runbook, no grad column, grade.py:77 treats every row as undergraduate; the graduate category and prediction dimension never enter any grade.",
  "reproduction": "Prose (both passages, verbatim):\n- templates/ta-runbook.md:24 \u2014 \"Record statuses in `status.csv` (`student_id,status,note`).\"\n- tools/grade.py:15 \u2014 \"status.csv:    student_id,status,grad,note\"  (identical in tools/runner.py:81)\n\nOwnership gap:\n$ grep -ni \"grad\" templates/ta-runbook.md  -> only substring hits inside \"graded\"/\"grading\" (lines 3,7,19,32,42,44,47,50,52,56,57,58,64); no mention of grad\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J11-ta-clarity-11",
  "judge": "J11",
  "severity": "major",
  "file": "templates/calibration-checklist.md:8",
  "claim": "Step order requires seeds.secret.json before the checklist mentions creating it.",
  "evidence": "L8 `--create-slots` (runner.py:72 exits \"seeds file missing\"); seeds first appear at L33 under Lock. L10 \"twist categories summing to half the hidden weight\" vs tools/README.md:54 \"half of all non-grad weights\".",
  "failure": "A professor working top-down stops at Setup or invents seeds; two readers apply different twist-weight rules.",
  "reproduction": "1. Ordering. Copied a real project.json into a clean dir and ran the Setup step verbatim:\n\n  cd /tmp/j11 && cp .../examples/01-morris-type-b/part-I/project.json .\n  python3 tools/runner.py --project project.json --create-slots\n\nOutput:\n  seeds file missing: /private/tmp/j11/seeds.secret.json (create {\"seeds\": [s1, s2, s3]}; keep it secret until grades are out)\n  EXIT=1\n\nSource: /Users/xaviermendez\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J11-ta-clarity-12",
  "judge": "J11",
  "severity": "minor",
  "file": "templates/ta-runbook.md:52",
  "claim": "Missing-ledger handling for Type B contradicts the handout and leaves 1\u20132 entries undefined.",
  "evidence": "\"already reflected in nothing ... not as a separate penalty, unless the handout said otherwise\"; handout-type-b.md:17 \"A specification that does not produce a ledger entry has not specified the task.\"",
  "failure": "One TA penalises, one does not.",
  "reproduction": "Read-only inspection, no fixtures needed.\n\n$ sed -n '52p' templates/ta-runbook.md\n\"Check the ledger: every Type B submission you ran should show three entries tagged `grading-k1..k3`. A submission with none means its specification never told the harness to sign; that is already reflected in nothing, so note it in the written-component `note` column for the professor. It is graded behavior only thr\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J12-J12-2",
  "judge": "J12",
  "severity": "blocker",
  "file": "templates/ta-runbook.md:47",
  "claim": "The runbook's grade.py command omits --written and --milestone, which tools/README.md and examples require, and never produces milestone.csv or written.csv.",
  "evidence": "ta-runbook.md:47 \"python3 tools/grade.py --project project.json --runs runs/ --status status.csv > gradebook.csv\"; ta-runbook.md:50 \"Enter the four (or three) dimension scores in `gradebook.csv`\"; tools/README.md:9 \"runner records plus `status.csv`, `written.csv`, `milestone.csv` into `gradebook.csv`\"; tools/grade.py:100 total = sum of hidden, ms_score, wr_score where blank inputs are skipped.",
  "failure": "TA follows Day 2 literally: gradebook.csv `total` = hidden score only (30 of 100 points missing for every student), scores typed into the CSV afterward are never summed; Day 3 sends that file to the professor.",
  "fix": "Add milestone.csv/written.csv steps and the full grade.py command from tools/README.md.",
  "reproduction": "Read /Users/xaviermendez/develop/course-ai-project-framework/templates/ta-runbook.md, tools/grade.py, tools/README.md.\n\ngrep -rn \"written.csv\\|milestone.csv\" templates/ examples/ framework.md README.md\n  -> only hit: examples/02-astar-type-a/README.md:56 (full command). No hit in templates/ta-runbook.md.\ngrep -n \"milestone\\|written\" templates/ta-runbook.md\n  -> lines 44, 50, 52 only, all \"written\"\u2026",
  "reason": "blocker third refuter did not run"
 },
 {
  "id": "J12-J12-5",
  "judge": "J12",
  "severity": "minor",
  "file": "templates/handout-type-b.md:17",
  "claim": "Student-facing bold line uses the avoided term 'agent' for the harness; copied into examples 01 and 03 handouts.",
  "evidence": "\"Your specification must tell the agent to sign the course ledger\" (handout-type-b.md:17, examples/01 handout.md:17, examples/03 handout.md:17); CONTEXT.md:41 \"_Avoid_: agent\". The grill/ADR/CONTEXT uses (\"agent loop\", \"CLI coding agent\") are legitimate; this one is drift.",
  "failure": "Students read 'agent' in the one line the ADR says must be unmistakable while every other document says 'harness'.",
  "fix": "agent \u2192 harness.",
  "reproduction": "Read /Users/xaviermendez/develop/course-ai-project-framework/docs/review/brief.md (severity scale, settled decision 28 on fixed vocabulary).\n\n$ sed -n '17p' templates/handout-type-b.md\n**Your specification must tell the agent to sign the course ledger with your student ID (and your partner's, if any) and the nonce from the page.** A specification that does not produce a ledger entry has not specif\u2026",
  "reason": "verifier did not run (matters)"
 },
 {
  "id": "J12-J12-12",
  "judge": "J12",
  "severity": "minor",
  "file": "templates/student-primer.md:105",
  "claim": "'last year's programs' contradicts the dated account (Spring 2026, i.e. this year).",
  "evidence": "student-primer.md:105 \"reproduce last year's programs from prompts\"; framework.md:17 \"The previous version of this project (Spring 2026...)\"; primer dated 8 September 2026.",
  "failure": "Students infer the failure-pattern table comes from an older, different project.",
  "fix": "last year's \u2192 last semester's.",
  "reproduction": null,
  "reason": "verifier did not run (reproduce)"
 },
 {
  "id": "CRITIC-C-1",
  "judge": "CRITIC",
  "severity": "major",
  "file": "tools/runner.py:147",
  "claim": "The generated opencode.json sets no temperature, so OpenCode sends its own model default on every request and the 0.2/0.6/1.0 best-of-K schedule never reaches the model \u2014 the one vendor claim the whole fairness argument rests on, and no judge verified it against a vendor page.",
  "evidence": "runner.py:147-159 `cfg = {\"$schema\"\u2026, \"provider\": {\"ollama\": {\u2026\"options\": {\"baseURL\": \u2026}}}, \"model\": f\"ollama/{slot_model}\", \"permission\": {\u2026}, \"share\": \"disabled\"}` \u2014 no `temperature` and no `agent` key. Temperature is set only Ollama-side at runner.py:106 `modelfile = f\"FROM {p['base_model']}\\nPARAMETER temperature {t}\\nPARAMETER seed {s}\\n\"`. Vendor page https://opencode.ai/docs/agents/ (fetched 2026-09-08): \"If no temperature is specified, OpenCode uses model-specific defaults; typically 0 f\u2026",
  "failure": "TA runs `runner.py --project project.json --submissions submissions --out runs/`. OpenCode sends temperature 0.55 (its Qwen default) in every /v1 request, which overrides the slot model's Modelfile default. All three K slots run at 0.55 and differ only by seed. framework.md:139 (\"at temperatures 0.2, 0.6 and 1.0, in that order, identical for every student. This is the fairness argument in one sentence\"), framework.md:111 criterion 4, and student-primer.md:19 (\"three times, at temperatures 0.2, 0\u2026",
  "fix": "Write `\"agent\": {\"build\": {\"temperature\": <slot t>}}` (or the top-level equivalent) into the generated opencode.json and add a calibration box that reads back the temperature actually sent.",
  "reproduction": null,
  "reason": "verifier did not run (reproduce)"
 },
 {
  "id": "CRITIC-C-2",
  "judge": "CRITIC",
  "severity": "blocker",
  "file": "tools/runner.py:208",
  "claim": "The Type B workdir copy has only three ignore patterns, so whatever the `--submission` path contains \u2014 including `seeds.secret.json`, the hidden suite, the reference solution, and prior `runs/` \u2014 is copied into /work, where the harness executes the student's specification with bash and edit allowed. The repo ships the proof.",
  "evidence": "runner.py:208 `shutil.copytree(sub_dir, workdir, ignore=shutil.ignore_patterns(\"PROCESS.md\", \"WRITTEN.md\", \".git\"))`. `ls -a examples/03-serialization-type-b-research/runs/03-serialization-type-b-research/k1-work/` \u2192 `.gitignore canonical handout.md project.json README.md reference resource runs seeds.secret.json submissions tests`. `cat \u2026/k1-work/seeds.secret.json` \u2192 `{\"seeds\": [4127, 88213, 190531]}`, identical to the project's secret seeds file. `find examples -name seeds.secret.json` returns\u2026",
  "failure": "Any `--submission` path that is not a bare per-student folder puts the secret seeds and every hidden expected output at /work. The calibration checklist (line 16) already tells the professor to pass a non-student path (`--submission reference/`), and the shipped example-03 run was made with the project directory itself \u2014 the runner accepted it silently. A specification containing `cat seeds.secret.json` or `cat tests/hidden/*/*.out.json` then reads the secrets that ADR-0002 says must stay secret\u2026",
  "fix": "Refuse a `sub_dir` that contains project.json/seeds file; add `seeds.secret.json`, `runs`, `tests`, `reference` to the ignore patterns.",
  "reproduction": "1. Read tools/runner.py:206-208, :122-137, :158, :310-316 \u2014 copytree ignores only PROCESS.md/WRITTEN.md/.git; no check on sub_dir; workdir mounted rw at /work with bash+edit allowed.\n\n2. Confirmed the repo evidence:\n$ ls -a examples/03-serialization-type-b-research/runs/03-serialization-type-b-research/k1-work/\n.gitignore canonical handout.md project.json README.md reference resource runs seeds.se\u2026",
  "reason": "blocker third refuter did not run"
 }
]